{
    "development": {
      "database": "DB_ACCOUNTING",
      "username": "UsersDev",
      "password": "${POSTGRES_PASSWORD}",
      "host": "localhost",
      "port": 3000,
      "gateway": 8080,
      "dialect": "postgres",
      "jwtSecret": "${JWT_SECRET}"
    }
  }